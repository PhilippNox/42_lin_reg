
standardization(depend_on):

	from sklearn import preprocessing
	import numpy as np
	scaler = preprocessing.StandardScaler().fit(np.array(depend_on).reshape(-1, 1))
	X_scaled = scaler.transform(np.array(depend_on).reshape(-1, 1))
	print(scaler.mean_)
	print(X_scaled)

